pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'echo "Hello World - Kevin Bodie"'
                sh 'echo Here we go.....'
                sh 'echo docker --version*****'
                sh 'docker --version'
                sh 'docker --version'
                sh 'docker --version'
                sh 'pwd'
                sh 'whoami'
                sh 'cd ~'
                sh 'ls -al'
                sh 'cd Software*'
                sh 'ls -al'
            }
        }
    }
}
